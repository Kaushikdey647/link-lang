"""Deprecated — safe to delete (frontend_only branch).

Already dead before this branch (imported the long-removed ui/indexing.py,
never mounted by api/app.py). Superseded entirely by the Next.js port
(frontend/). See CHANGELOG.md.
"""
